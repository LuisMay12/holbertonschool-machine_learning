#!/usr/bin/env python3
"""Module that defines np_matmul, which
performs matrix multiplication with NumPy."""


import numpy as np


def np_matmul(mat1, mat2):
    """Return the matrix multiplication
    of two NumPy arrays."""
    return np.matmul(mat1, mat2)
