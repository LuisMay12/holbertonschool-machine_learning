#!/usr/bin/env python3
"""Module that defines np_shape, which returns the
shape of a NumPy array."""

def np_shape(matrix):
    """Return the shape of a NumPy ndarray as a
    tuple of integers."""
    return matrix.shape
